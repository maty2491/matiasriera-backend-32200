head
    meta(charset='UTF-8')
    meta(http-equiv='X-UA-Compatible' content='IE=edge')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')
    title Productos con PUG
    link(href='https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css' rel='stylesheet' integrity='sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65' crossorigin='anonymous')

.container
    table.table.align-middle.mb-0.bg-white
        thead.bg-light
            tr
                th Nombre
                th Precio
                th Imagen
        tbody
            - if(products.length &gt; 0) {
            - products.forEach(product=&gt; { 
            tr
                td
                    .d-flex.align-items-center
                        .ms-3
                            p.fw-bold.mb-1
                                - product.name
                td
                    p.fw-normal.mb-1 $ - product.price
                td
                    img.rounded-circle(src='- product.thumbnail' alt='' style='width: 45px; height: 45px')
                        - }) 
                        - }else {
                h1  No hay productos cargados
                    -}
    button#formButton.btn.btn-primary.mt-3 Volver a carga de productos
script.
    const formButton = document.getElementById('formButton')
    formButton.addEventListener('click', (e) => {
    window.location = "/pug"
    })